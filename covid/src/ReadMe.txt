DataBase.java file acts as Database for storing the data.

Person.java file acts to collect data from the user, validate the data,then insert into DataBase.java.array;

VaccineData.java file acts to collect vaccine data and adds to the vaccineAvailable int he database.